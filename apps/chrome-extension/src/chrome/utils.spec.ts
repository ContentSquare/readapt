import { getStoredSettings } from './utils'
import { settings } from '../../tests/fixtures/settings'

describe('chrome utils', () => {
  describe('getStoredSettings()', () => {
    describe('when storage contains settings', () => {
      it('should return the settings', () => {
        
      })
    })
  })
})
